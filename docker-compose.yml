services:
  jekyll:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - .:/site
      - bundle:/usr/local/bundle
    command: bundle exec jekyll serve --host 0.0.0.0 --port 4000 --livereload

volumes:
  bundle:
