# AI 한 접시 작업 기록

이 문서는 공개 블로그 본문과 분리된 운영 기록입니다. 날짜는 KST 기준입니다.

## 2026-09-11 — 발행 규칙 v1과 기존 글 정리 상태

### 발행 규칙 v1 운영 반영

- PR #21을 병합해 발행 규칙의 비소급 원칙과 선언 기반 엄격 CI를 운영 반영했습니다.
- 규칙 버전 `1`의 발효일은 2026-09-11입니다.
- 새 규칙 또는 규칙 수정은 발효일 이후 새로 발행하는 글에만 적용합니다.
- 새 글은 front matter에 현재 규칙 버전(`editorial_rules`)을 선언하고, 선언된 버전에 따라 엄격 CI 검사를 통과해야 합니다.
- 기존 공개 글은 당시 발행 규칙으로 보존하며, 일반 CI의 경고·차단 대상이 아닙니다.
- 기존 글 전체의 `--report` 검사는 필요할 때만 수동 품질 조사로 실행합니다.

### 소급 적용 예외

다음 경우만 대상 글·사유·수정 범위를 기록한 뒤 개별 소급 적용을 검토합니다.

1. 법률·저작권·개인정보·안전 문제
2. 독자의 판단에 영향을 주는 중대한 사실 또는 출처 오류
3. 이미지·링크·본문·번역처럼 독자의 열람·이용을 막는 기능 장애
4. 보안·접근성·검색 색인처럼 사이트 운영상 즉시 보정이 필요한 문제
5. 사용자가 특정 기존 글 또는 명확한 범위를 지정해 직접 요청한 수정

표기 통일, 시각 스타일 개선, 새 자동 검사 도입은 소급 적용 예외가 아닙니다.

### 기존 글 편집 규칙 정리

- 2단계 소제목 정리는 완료됐습니다. 한·영 기존 글 16개의 `h2` 67곳에 문장부호를 보완했고, PR #18로 운영 반영했습니다.
- 3단계 기존 삽화 표기 통일은 중단·복구 완료 상태입니다. 외부 PNG를 참조하는 SVG 래퍼가 브라우저에서 삽화를 표시하지 않는 문제가 발생했고, PR #20으로 해당 변경 전체를 되돌렸습니다.
- 기존 삽화와 기존 AI 생성 표기는 원래 상태로 복원됐습니다.
- 비소급 원칙에 따라 기존 글 대상 3단계, 4단계(규칙 선언 추가), 5단계(기존 글 보고서 0건)는 더 진행하지 않습니다.
- 기존 글의 수동 `--report` 기준선은 AI 이미지 선언 미비 관련 경고 8건입니다. 이는 일반 CI를 차단하지 않습니다.

### 검증·운영 상태

- PR #21 병합 커밋: `4b36d3bba6ead85f5a7b771c904c350e1cdf01a1`
- PR #20 복구 병합 커밋: `308352ffee83a7470ce6227e71f98ad9967d4ef4`
- PR #21의 `jekyll-build`, `validate-site`, Pages 배포 워크플로는 성공했습니다.
- 규칙 v1 적용 뒤 엄격 검사 결과: 기존 공개 글 18편 중 선언된 규칙 버전 글 0편, 경고 0건입니다.

## 다음 발행부터 적용할 항목

- 한국어·영어 글을 같은 발행 단위로 준비하고, Preview에서 함께 검토합니다.
- 새 공개 글은 발행 시점의 `editorial_rules` 버전을 선언합니다.
- AI 생성 SVG 삽화가 있으면 `ai_generated_images`에 경로를 선언하고, 이미지 내부 우측 하단에 `AI로 생성한 이미지` 글씨만 둡니다.
- 규칙을 수정할 때는 `PUBLISHING_RULES.md`의 규칙 버전 기록에 버전·발효일·변경 내용을 추가합니다.
